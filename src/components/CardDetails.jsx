/* import React from "react";
import "./CardDetails.css";

const CardDetails = ({user}) => {
	// const user = props.user;
	return (
		<div className="tarea">
			<div className="img-container">
				<img src={user.image} alt={`avatar de ${user.name}`} />
			</div>
			<div className="personal section">
				<p className="personal__name">Name: {user.name}</p>
				<h2 className="personal__status">Estatus:{user.status}</h2>
				<p className="personal__address">Type: {user.type}</p>
			</div>
			<div className="cardepisode">
			<p className="social__intro">Especie:{user.species}</p>
			<p className="social__intro">Especie:{user.species}</p>
			</div>
			<div className="buttonHome">
				<button className="home">HOME</button>
			</div>
		</div>
	);
};

export default CardDetails; */